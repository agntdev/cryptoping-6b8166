[
  {
    "name": "Set quiet hours",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Open quiet-hours configuration UI (buttons for common ranges + custom time input)."
            }
          }
        ],
        "send": {
          "callback": "settings:quiet_hours"
        }
      }
    ]
  }
]
