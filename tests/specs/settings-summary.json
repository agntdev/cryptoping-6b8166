[
  { "name": "morning summary settings open", "steps": [
    { "send": { "callback": "summary:open" }, "expect": [{ "method": "sendMessage", "payload": { "text": "Morning summary is off." } }] }
  ] }
]
